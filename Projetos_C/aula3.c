#include <stdio.h>

int main(){

    // variable = A reusable container for a value.
    //            Behaves as if it is the value it contains.

    float gpa = 2.5;
    float price = 19.99;
    float temperature = -10.1;

    printf("Your gpa is %.1f\n", gpa);
    printf("The prince is $%.2f\n", price);
    printf("The temperature is %.1f°F C\n", temperature);

    return 0;
    
}